# Custom IAR Academy Board

`custom_aca` board is used to demonstrate how to create custom boards. It is
in fact a simplified version of the IAR STM32F429II-ACA board, so the
`example-application` can be run on that development kit when using
`custom_aca`.
